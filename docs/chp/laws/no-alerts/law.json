{
  "name": "no-alerts",
  "intent": "Prevent alert() calls — use toast notifications, modal dialogs, or error boundaries",
  "created": "2026-04-25T07:52:30Z",
  "severity": "warn",
  "failures": 0,
  "tightening_level": 0,
  "hooks": ["pre-commit", "pre-push", "pre-tool"],
  "enabled": true,
  "checks": [
    {
      "id": "alert-call",
      "type": "pattern",
      "config": {"pattern": "alert\\("},
      "severity": "warn",
      "message": "Use toast notifications or modal dialogs instead of alert()"
    },
    {
      "id": "alert-guidance",
      "type": "agent",
      "config": {
        "prompt": "RULE: Never use alert(). Use toast notifications, modal dialogs, or proper error boundaries instead. alert() blocks the UI thread and provides a poor user experience."
      },
      "severity": "block",
      "message": "Use toast notifications or modal dialogs instead of alert()"
    }
  ]
}
